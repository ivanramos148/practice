function robo(userInput) {

  //Any characters A-Z will not work
  if(userInput.match(/[1-9]/i)) {

  //if user inputs the num 0 robot will replace it with BOOP
}if (userInput === "1") {
    userInput = "boop";
    return userInput;
}











  }
