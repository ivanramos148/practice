

};
$(document).ready(function() {
    $("#beep").submit(function(event) {
      even.preventDefault();
      var user = ("#user").val();
      $("answer").text(newArray.join(""));
      event.preventDefault();
});
});
